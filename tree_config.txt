# Voxel Tree System Configuration
# 体素树系统配置文件

[Generation]
# Tree generation parameters
tree_count = 50              # 生成树的数量
min_slope = 5.0             # 最小坡度（度）
max_slope = 45.0            # 最大坡度（度）
min_spacing = 20.0          # 树之间最小间距
random_seed = 42            # 随机种子

[Region]
# Spatial region filtering (in normalized 0-1 terrain coordinates)
# Temple hillside is at center (0.5, 0.75), we want trees around it
enable_region_filter = true  # 启用区域过滤
region_min_x = 0.3          # 最小X坐标（归一化）
region_max_x = 0.7          # 最大X坐标（归一化）
region_min_z = 0.55         # 最小Z坐标（归一化）- 只在山坡区域
region_max_z = 0.95         # 最大Z坐标（归一化）
min_height = 40.0           # 最小生成高度（避开湖泊和平原）
max_height = 280.0          # 最大生成高度（避开寺庙平台）

[TreeShape]
# Tree shape parameters
min_height = 12             # 最小树高（方块数）- 巨大的树
max_height = 18             # 最大树高（方块数）- 超高的树
canopy_radius = 6           # 树冠半径（方块数）- 巨大的树冠
canopy_height = 7           # 树冠高度（方块数）- 超高的树冠
canopy_density = 0.85       # 树冠密度 (0.0-1.0) - 非常茂密
spherical_canopy = true     # 是否使用球形树冠（true/false）
block_size = 4.0            # 每个方块的大小（世界单位）- 巨大的方块

[Growth]
# Tree growth parameters
enable_growth = true        # 是否启用生长系统
growth_speed = 0.02         # 生长速度（每秒增长2%，50秒长满）
initial_growth = 0.5        # 初始生长进度 (0.0-1.0, 0=种子, 1=成熟)
